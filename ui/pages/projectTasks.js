import React from 'react';
import ProjList from '../components/projList'

const HelloMessage = () => {
      return (
        <div>
        <ProjList/>
        </div>
      );
  }

  export default HelloMessage;